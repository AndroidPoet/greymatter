#!/usr/bin/env python3
"""claude++ - Claude CLI with persistent memory"""
import sys
from pathlib import Path

# Add parent dir to path
sys.path.insert(0, str(Path(__file__).parent.parent))

from aiplusplus.wrapper import main
sys.exit(main())
